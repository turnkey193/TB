# 母資料夾 .gitignore（白名單模式：預設全忽略，再顯式 !xxx 放行）
# 子專案（如 tb-meeting/、人資薪資打卡系統/）天生就被擋掉，不會進母 git
# 各子專案有自己的 .git/ 與 .gitignore

# === 預設忽略所有東西 ===
*
.*

# === 母資料夾層的說明與設定 ===
!.gitignore
!CLAUDE.md

# === 共用資源（跨子專案使用） ===
# Supabase migrations + schema 文件
!supabase/
!supabase/**
supabase/.temp/
!docs/
!docs/**

# Drive Explorer（特例：目前還沒分離出去，暫時留在母 git）
!drive工具/
!drive工具/drive-explorer/
!drive工具/drive-explorer/**

# 週會管理系統 tb-meeting/ 已改成獨立子專案 repo (17310a3-png/TB-meeting)
# 不在母 git 追蹤範圍（天生就被 * 擋掉，這裡不 whitelist）
